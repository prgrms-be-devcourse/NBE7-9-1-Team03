spring:
  datasource:
    url: jdbc:mysql://34.64.170.30:3306/coffee?serverTimezone=Asia/Seoul&characterEncoding=UTF-8
    username: coffee
    password: bcTC4Coffe!
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update   # 운영에서는 update보다는 validate 권장
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect
    show-sql: true